import '../../../node_modules/particles.js/particles.js';

particlesJS.load('particles-js', '../../files/particles.json', function () {
    // const pJSIndex = 0;
    // const pJS = pJSDom[pJSIndex].pJS;
    // pJS.particles.number.value = 60
    // pJS.particles.line_linked.opacity = 0.2
    // pJS.interactivity.events.resize = true
    // pJS.fn.particlesRefresh()
    
    // gui.add(pJS.particles.number, 'value').step(1).min(10).max(100).name('particles number')
    //     .onChange(() => {
    //         pJS.fn.particlesRefresh();
    //     });


    // gui.add(pJS.particles.size, 'value').step(1).min(1).max(50).name('particles size')
    //     .onChange(() => {
    //         pJS.fn.particlesRefresh();
    //     });

    // gui.add(pJS.particles.shape, 'type', ["circle", "edge", "triangle", "polygon", "star"]).name('particles type')
    //     .onChange(() => {
    //         pJS.fn.particlesRefresh();
    //     });

    // gui.addColor(pJS.particles.color, 'value').name('particles color')
    //     .onChange(() => {
    //         pJS.fn.particlesRefresh();
    //     });

});